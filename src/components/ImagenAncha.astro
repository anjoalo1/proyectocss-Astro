---

interface Props {
	direccion: string;
    alto:string;
 /*    myArray: Array<[]>; */
}

const { direccion, alto } = Astro.props;

---




<div class="my-image">
    <img src={direccion} height={alto} />
</div>


<style>
    .my-image{
        text-align: center;
        margin: 0 auto;
        min-width:200px;
        max-width:700px;
        overflow:auto;
    }
    </style>
